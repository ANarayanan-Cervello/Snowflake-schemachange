CREATE OR REPLACE SCHEMA "TESTING"."RCM_STAGING_DV"
COMMENT='Schema to store all the staging objects';

CREATE OR REPLACE SCHEMA "TESTING"."RCM_RAW_DV"
COMMENT='Schema to store all the raw vault objects';